import styles from './home-banner.module.scss';

const HomeBanner = () => (
  <div className={styles.banner}>
    <h1 className={styles.banner_text}>kng.nz</h1>
  </div>
);

export default HomeBanner;
